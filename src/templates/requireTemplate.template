const __JSV_RUNTIME = (typeof require === "function" ? require("%VM_PATH%") : null)
    || (typeof globalThis !== "undefined" && globalThis.JSVM ? globalThis.JSVM : null);

if (!__JSV_RUNTIME) {
    throw new Error("JSVM runtime is not available");
}
